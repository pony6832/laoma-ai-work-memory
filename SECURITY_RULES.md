# 安全規則

## 禁止寫入
- 密碼、API Key、Access Token、Cookie、私鑰
- 身分證號、信用卡完整資料、銀行憑證
- 未脫敏的公司機密原文、客戶個資、未授權私人檔案

## 允許寫入
- 裝置型號、作業系統、軟體版本、非敏感設定
- 專案目標、進度、錯誤摘要、檔名與公開技術資訊
- 已脫敏路徑與環境描述

## 規則
1. 敏感值只記變數名稱，不記實際內容。
2. 公司與私人專案分開標示。
3. 不確定是否敏感時，先不提交。
4. 所有自動化輸出提交前先檢查 `git diff`。

## 自我生長記憶安全閘門

1. 自動化只能建立 `.memory-growth/` 本機候選，不得直接建立正式記憶。
2. 候選含疑似密碼、Key、Token、Cookie、私鑰、電子郵件、身分證號或付款卡號時必須阻擋。
3. 候選必須記錄來源、範圍、可信度與內容指紋，並在批准前檢查重複。
4. 只有老馬明確批准的候選可以寫入 `knowledge/` 並進入 Git。
5. 過期記憶只標記待複查，不自動刪除或覆寫；修正必須保留來源與取代關係。
6. Git 提交前必須通過 `python tools/memory_growth.py audit --staged`。
