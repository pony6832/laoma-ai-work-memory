# 正式長期記憶

此目錄只保存通過人工批准的長期記憶。候選記憶位於本機且被 Git 忽略的 `.memory-growth/`，不得直接同步。

正式記憶分類：

- `fact`：可驗證事實。
- `decision`：重要決策及其理由。
- `pattern`：反覆出現、可重用的經驗。
- `playbook`：已驗證的操作流程。
- `preference`：使用者明確確認的偏好。
- `project`：會隨時間失效的專案脈絡。

每筆記憶包含來源、可信度、指紋、驗證日期與可選的到期日。到期記憶只會被稽核標示，不會自動刪除。
